<!-- MedelaBIOS-FileVersion: 4.0.0 -->
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Device care"
 Width="600" Height="510" MinWidth="380" MinHeight="320" FontFamily="Segoe UI" FontSize="14"
 WindowStartupLocation="CenterScreen" WindowStyle="None" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}" ResizeMode="CanResize" UseLayoutRounding="True">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
  </shell:WindowChrome.WindowChrome>

 <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"><Grid><Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
      <Grid Height="48"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Grid Margin="14,0,8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="24"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
          <Image Width="24" Height="24" Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=Window}}" AutomationProperties.Name="Application icon"/>
          <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"/>
        </Grid>
        <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
          <Button x:Name="CaptionMinimize" shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CaptionButton}" Content="&#x2014;" ToolTip="Minimize window" AutomationProperties.Name="Minimize window"/>
          <Button x:Name="CaptionMaximize" shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CaptionButton}" Content="&#x25A1;" ToolTip="Maximize window" AutomationProperties.Name="Maximize window"/>
          <Button x:Name="CaptionClose" shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CaptionButton}" Content="&#x00D7;" ToolTip="Close window" AutomationProperties.Name="Close window"/>
        </StackPanel>
      </Grid>
    </Border>

  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
   <StackPanel Margin="24">
    <Image x:Name="Logo" Height="36" MaxWidth="180" Stretch="Uniform" HorizontalAlignment="Left" Visibility="Collapsed" AutomationProperties.Name="Company logo"/>
    <TextBlock x:Name="Company" FontWeight="SemiBold" Margin="0,8,0,14"/>
    <Image x:Name="Banner" Height="70" Stretch="UniformToFill" Visibility="Collapsed" Margin="0,0,0,12" AutomationProperties.Name="Company banner"/>
    <TextBlock x:Name="Heading" FontSize="23" FontWeight="SemiBold" Margin="0,0,0,10"/>
    <TextBlock x:Name="Purpose" Margin="0,0,0,12"/>
    <Border x:Name="StatusCard" Background="{DynamicResource SurfaceBrush}" Padding="14" CornerRadius="8" Margin="0,0,0,12"><StackPanel>
     <TextBlock x:Name="StatusText" AutomationProperties.LiveSetting="Polite"/>
     <ProgressBar x:Name="Progress" Height="8" Margin="0,12,0,0" IsIndeterminate="True" Visibility="Collapsed" AutomationProperties.Name="BIOS preparation activity"/>
     <TextBlock x:Name="Deadline" FontWeight="SemiBold" Margin="0,10,0,0"/>
     <TextBlock x:Name="Remaining" Margin="0,5,0,0"/>
    </StackPanel></Border>
    <StackPanel x:Name="SchedulePanel" Visibility="Collapsed" Margin="0,0,0,12">
     <TextBlock Text="Choose an installation time on this computer (local time)." FontWeight="SemiBold"/>
     <TextBlock Text="Choose at least five minutes from now, before the deadline. Keep the computer signed in and plugged in. The restart warning starts after preparation." Margin="0,6,0,8"/>
     <WrapPanel>
      <DatePicker x:Name="InstallDate" Width="155" Margin="0,0,10,8" AutomationProperties.Name="Installation date"/>
      <ComboBox x:Name="InstallTime" Width="125" IsEditable="True" Margin="0,0,0,8" AutomationProperties.Name="Installation time, 24-hour HH:mm"/>
     </WrapPanel>
     <TextBlock x:Name="ScheduleError" Foreground="{DynamicResource ErrorBrush}" Margin="0,0,0,8" AutomationProperties.LiveSetting="Assertive"/>
     <WrapPanel><Button x:Name="ConfirmSchedule" Content="_Confirm schedule"/><Button x:Name="Back" Content="_Back"/></WrapPanel>
    </StackPanel>
    <TextBlock x:Name="Power" Margin="0,0,0,12"/>
    <TextBlock x:Name="Support" Foreground="{DynamicResource MutedBrush}" FontSize="12"/>
   </StackPanel>
  </ScrollViewer>
  <Border x:Name="ActionBar" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Grid.Row="2" Padding="24,12"><WrapPanel>
   <Button x:Name="Primary" Style="{DynamicResource PrimaryAction}" Content="_Install Now" AutomationProperties.Name="Install Now"/>
   <Button x:Name="Schedule" Content="_Schedule Install" AutomationProperties.Name="Schedule Install"/>
   <Button x:Name="Secondary" Content="_Defer" AutomationProperties.Name="Defer"/>
  </WrapPanel></Border>
 </Grid></Border>
</Window>
