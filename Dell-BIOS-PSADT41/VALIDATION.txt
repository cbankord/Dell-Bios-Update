Validation of 2026-09-16 changes

PASS: 45 assertions using PowerShell 7.6.6 on Linux:
- Parse all supplied PS1/PSD1 source files, including the wrapper and tests.
- Mocked registry: create once, retain previous values, reject damaged missing-Status state.
- Required password file/placeholder/read/parse-error sanitization; optional omission.
- Native argument quoting for spaces, dollar signs, quotes and backslashes.
- Battery 51% on AC passes; 50%, unplugged AC and unknown AC block.
- No-user and Silent mode retries; cooldown after the third deferral; final notice.
- Staging restart timer and remaining time; final 15 minutes; shared text restored.
- Existing transaction skips deferral; UI failure still returns 3010.
- No-user staged case skips restart UI; compliant firmware exits without prompting.
- Child prerequisite Retry passes through without a restart prompt.
PASS: git diff --check.
Manual review: PSADT 4.1.0 welcome/restart parameter sets and deferral history.
The restart mock enforces separate Countdown and SilentRestart parameter sets.

Reproduce from Dell-BIOS-PSADT41:
  pwsh -NoProfile -File ./Tests/Test-Deployment.ps1
On a Windows pilot, additionally test the real registry writer in an isolated HKCU key:
  powershell.exe -NoProfile -File .\Tests\Test-Deployment.ps1 -WindowsRegistryIntegration
The HKCU integration mode was NOT run here. It never accesses the deployment key.
The harness uses inert configuration/hash/password fixtures and never flashes firmware.
The older 53-assertion validation below is historical, not a rerun of this revision.

BLOCKERS / UNVERIFIED:
- Repository SHA256 is 65 characters, not 64. Recompute from the approved EXE.
- Existing model/EXE compatibility remains unverified; PackageReviewed remains false.
- No real password or BIOS executable is present; Intune scripts cannot be built yet.
- Windows PowerShell 5.1, actual PSADT UI, Intune delivery/timers, Dell updater,
  Windows Registry/Task Scheduler, Authenticode and BitLocker require a pilot.
- Logoff can remove UI; PSADT 4.1 has a no-user restart race. Intune is the fallback.
- AC is checked at staging; the countdown cannot ensure AC remains connected.
- Shared password delivery is plaintext in the local package and native arguments.

--- Historical initial-source validation follows ---

Validation performed 2026-09-15

PASS: 53 assertions using PowerShell 7.4.6 on Linux.
- PowerShell parser accepted all supplied PS1 and PSD1 files.
- Numeric version order and normalization; malformed/legacy versions rejected.
- Unreviewed/default configuration rejected.
- Unsafe reboot count, bad hash, traversal filename, low battery threshold,
  invalid prerequisite version, empty model and excessive marker lifetime rejected.
- Exact model/manufacturer acceptance and rejection using mocked CIM.
- Payload hash rejection and publisher/signature rejection using mocked
  Authenticode results (no real Windows certificate-chain validation here).
- Builder produced parseable standalone requirement, detection and audit scripts
  from an inert QA-only fixture with a mocked signature result.
- Detection subprocesses checked actual equal/newer/older versions, staged same
  boot, staged after reboot, expired/future markers, mismatched hash/target,
  failed/launching transactions and wrong model.
- Detection success emitted STDOUT; detection failures did not emit success text.
- PSADT integration parameter names checked against official 4.1.0
  Start-ADTProcess.ps1 source.

NOT TESTED:
- Windows PowerShell 5.1 runtime execution (source targets its syntax/API surface).
- PSADT 4.1 end-to-end execution, Intune install/detection/restart scheduling.
- Dell hardware, actual BIOS EXE, AC/battery telemetry, firmware boot sequence.
- Windows Authenticode validation, ACL operations, BitLocker escrow/suspension/
  resume, Registry and Task Scheduler integration.

No Dell BIOS binary or PSADT distribution is included. The default configuration
is intentionally blocked until the administrator supplies and reviews the model,
version, executable and hash. Pilot testing is required before production rollout.
