V2 validation performed 2026-09-16

PASS: 125 assertions across four fresh PowerShell 7.6.6 processes on Linux:
  39  Tests/Test-Deployment.ps1
  63  Tests/V2/Test-Scheduler.ps1
   9  Tests/V2/Test-Transport.ps1
  14  Tests/V2/Test-Guards.ps1
Counts include parser assertions in more than one suite; they are not 125 unique
end-to-end scenarios. The optional WindowsRegistryIntegration mode was not run.

Covered:
- Parse all PS1/PSD1 sources and well-formed XAML.
- Registry writer mocked create-once behavior and actionable damaged-state errors.
- Local password requirements, sanitized parse errors, and native argv escaping.
- AC/unknown-power and 50% vs 51% battery gates using inert native/CIM telemetry.
- First delivered notice starts exactly 72 hours; enrollment/status alone does not.
- 100 deferrals, repeated acknowledgements and reschedules preserve the deadline.
- Reject past/late/unzoned schedule times, unknown actions and extra fields.
- Deadline expiry disables deferral/scheduling; preparation locks further changes.
- Local/UTC conversion and both spring/fall daylight-saving ambiguities.
- Missing, corrupt and mismatched schedule state fails closed.
- Real temporary-file create/read/atomic replacement, without production paths.
- Bounded framed JSON roundtrip and invalid/truncated frame rejection.
- Compile Windows peer/session and input-desktop C# declarations without invoking them.
- Mocked engine: missed deadline warnings, pre-stage power holds, restoration,
  near-schedule launch, successful staging, final warning, power-loss restart hold,
  post-boot verification and blocked reflashing after failure/ambiguous crash.
- Actual restart guard with mocked BitLocker: wrong boot/hash/status, missing
  transaction, unowned suspension, active protection, locked OS volume and
  unexpected encryption changes all block a restart.
- A verified marker alone does not bypass post-boot health checks.

PASS: git diff --check.
Manual source/API review: PSADT 4.1 Start-ADTProcessAsUser, ScheduledTasks group
principals, named-pipe ACL/client access rights, and Windows session constants.
The client uses specific PipeAccessRights rather than GENERIC_WRITE, avoiding
an accidental request for the pipe-instance creation right.

Reproduce from Dell-BIOS-PSADT41, using separate processes:
  pwsh -NoProfile -File ./Tests/Test-Deployment.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Scheduler.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Transport.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Guards.ps1
On Windows, additionally run:
  powershell.exe -NoProfile -File .\Tests\Test-Deployment.ps1 -WindowsRegistryIntegration
  powershell.exe -NoProfile -STA -File .\Files\UI\Show-BiosUI.ps1 -Demo

NOT TESTED / MUST PILOT:
- Windows PowerShell 5.1 execution, WPF rendering, screen reader/scaling behavior.
- Actual Windows ACL inheritance, named-pipe ownership/impersonation and active
  session checks; task Group activation, logon, SYSTEM and user launch behavior.
- Intune packaging/detection delivery, competing tenant restart policies, sleep,
  locked desktop, logoff, app-blocked restart and controller recovery on Windows.
- Actual Dell executable/password, firmware staging/boot, Authenticode chain,
  recovery-key retrieval, BitLocker escrow/suspension/resume and hardware telemetry.
- A malicious client/administrator penetration test. The protocol and directory
  protections were reviewed in source, not certified by live security testing.

PACKAGE BLOCKERS (inherited and intentionally preserved):
- SHA256 is 65 characters. Recalculate it from the approved EXE; do not trim it.
- PB14250 model vs PA13250-named executable compatibility remains unverified.
- PackageReviewed=false; real BIOS EXE, password and PSADT distribution absent.
- Generated Intune scripts cannot be produced against an unapproved/missing EXE.

No production BIOS, protection state, scheduled task, restart or Intune assignment
was changed by these tests. Main remains v1; this implementation is on v2.
