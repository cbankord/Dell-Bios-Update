V4.1 selectable package output validation - 2026-09-18

PASS: 250 assertions across five isolated PowerShell 7.4.7 processes on Linux:
  61  Tests/Test-Deployment.ps1
 118  Tests/Test-Builder.ps1
  22  Tests/Test-BuilderClose.ps1
  15  Tests/V4/Test-BuilderOutput.ps1
  34  Tests/V2/Test-Cache.ps1
Run each suite in its own process from Dell-BIOS-PSADT41:
  pwsh -NoProfile -File <path above>
Counts include parser/fixture assertions, not independent Windows E2E scenarios.

Coverage includes complete inert source/optional-intunewin builds, old/new preset
output choices, blank/relative/UNC/missing/file/repository rejection, a chosen
destination containing spaces and square brackets, root preservation, manifest
and log destinations, only-new-child protection calls and cleanup that retains
previous builds and unrelated siblings. Square-bracket output initially exposed
two wildcard data-file reads; both now use LiteralPath and the full build passes.
The runtime manifest and Intune detection are generated from that actual output.

The new UI suite executes the actual folder-selection and review callbacks with
inert native-dialog/owner edges. Accept updates the field, Cancel retains the old
or blank value, failures permit manual entry, and resources are released in all
cases. It checks the picker owner/current folder and v4.1 caption/Build placement.
Existing async close tests prove successful/failed workers finish cleanup and
dispose credentials before closing. Cache and base/parser regressions pass.

PASS: git diff --check; all PS1/PSD1 sources parse; builder XAML is well formed.
NOT RUN: Windows PowerShell 5.1, actual WPF/folder dialogs/owner focus, DPI/keyboard/
screen-reader checks, native NTFS ACLs, Microsoft content-prep execution or Intune/
Dell device deployment. API edges are mocked; no BIOS or packaging EXE is run.

Required Windows pilot: choose/create/cancel/type a destination and load an old
preset; verify the Build tab, review, actual unique directory, Open output and
BuildManifest BuilderVersion 4.1.0. Test local drive roots and bracket/space paths,
parent/sibling permissions and failed-build cleanup. Validate custom chrome and
the owned picker at 100/150/200% scaling, and Close during an active build. Keep
the existing actual-custom-framework, firmware safety and recovery pilot gates.
Full package and matching detection rebuild deliver Cache.ps1 4.1.0; other runtime
tattoos retain their versions and accepted-work/transaction holds still apply.

Previous validation records follow.

V4.0.1 Install Now / PSADT parameter binding validation - 2026-09-17

PASS: 615 assertions across six isolated PowerShell 7.4.7 processes on Linux:
  60  Tests/Test-Deployment.ps1
 103  Tests/Test-Builder.ps1
  34  Tests/V2/Test-Cache.ps1
  54  Tests/V2/Test-SimpleDeployment.ps1
  57  Tests/V2/Test-Live.ps1
 307  Tests/V4/Test-ProcessBinding.ps1
Counts include parser/fixture assertions, not independent Windows E2E scenarios.
Run each suite in its own process from Dell-BIOS-PSADT41:
  pwsh -NoProfile -File <path above>

The new suite reconstructs inert advanced functions from official parameter
metadata for Start-ADTProcess and Start-ADTProcessAsUser at tags 4.1.0-4.1.8.
PowerShell's actual binder reproduces AmbiguousParameterSet for all three old
calls on 4.1.4-4.1.8 (including NoWait:$false), and accepts the corrected calls
on all nine releases. Earlier releases accept the old combination too.
The fixture records source URLs; Tests/Tools/Export-PSADTProcessParameters.ps1
regenerates it by parsing reviewed sources without importing/executing them.

The suite executes the real prompt, installer and staging wrappers, covering
synchronous UI control codes, preflight, asynchronous progress before worker,
actual result propagation for 0/3010/1618/60001, failed UI launch preventing worker
launch, failed worker launch retiring transient status, and distinct safe log
boundaries. No asynchronous call adds IgnoreExitCodes, Timeout or parent-kill
options. Existing live tests exercise UI loss while a worker continues and
guarded restart cancellation. Builder tests produce complete inert packages;
cache/flow tests retain state and replacement protections.

PASS: git diff --check and PowerShell parser coverage for all PS1/PSD1 files.
NOT RUN: native Windows PowerShell 5.1, actual custom PSADT module execution,
WPF, SYSTEM/user-session process creation, Intune, real BIOS, BitLocker or restart.
Framework bodies, default expressions and validation callbacks are not executed
by the metadata regression; Windows-only identity types have inert substitutes.
This proves the parameter-set correction, not native Windows end-to-end behavior.

Required pilot: rebuild with the same approved BIOS/settings and prepared PSADT
4.1 ZIP using BuilderVersion 4.0.1. Deploy the full Source/package and matching
new detection. On a pilot device validate Install Now -> visible preparation ->
actual worker result -> guarded restart warning -> post-boot verification. Retain
the existing power, credential, recovery and Intune restart-policy pilot gates.
Do not force runtime replacement when an accepted appointment or firmware
transaction is unresolved; follow OPERATIONS.md. No Main/v2/v3 change is included.

Previous validation records follow.

V4.0 scheduling option and custom window validation - 2026-09-17

PASS: 569 assertions across 12 fresh PowerShell 7.4.7 processes on Linux:
  58  Tests/Test-Deployment.ps1
 103  Tests/Test-Builder.ps1
  22  Tests/Test-BuilderClose.ps1
  14  Tests/V2/Test-Guards.ps1
  34  Tests/V2/Test-Cache.ps1
  54  Tests/V2/Test-SimpleDeployment.ps1
  13  Tests/V2/Test-Migration.ps1
  57  Tests/V2/Test-Live.ps1
  52  Tests/V2/Test-CachePermissions.ps1
  75  Tests/V2/Test-Scheduling.ps1
  24  Tests/V2/Test-ScheduledRecovery.ps1
  63  Tests/V4/Test-Experience.ps1
Counts include parser/fixture assertions, not independent Windows E2E scenarios.
Reproduce each suite in its own process from Dell-BIOS-PSADT41:
  pwsh -NoProfile -File <path above>

New coverage executes real builder/state/cache/deployment functions and actual
UI event callbacks, using inert files and modeled Windows boundaries:
- Complete generated packages with both Boolean values; defaults and old presets,
  explicit false round-trip, invalid Boolean strings, policy/build records and
  unchanged deferral/restart settings. Custom framework integration remains valid.
- New disabled schedules and reschedules fail before task/source/state mutation.
  Forged UI scheduling replies fail in the adapter and privileged deployment.
  Fresh disabled deployments defer without creating installation tasks.
- Changed incoming policy holds without changing cached policy, accepted state
  or task. The accepted source stages when due, unresolved firmware blocks
  replacement, and verified cleanup permits disabled policy activation with the
  exact original deadline. Existing due state under disabled policy repairs its
  task and proceeds only when power is safe.
- Enabled/disabled/predeadline/overdue UI paths, stale schedule/defer clicks,
  preview close, overdue timeout and close guard, live progress/restart minimize,
  Restart Now phase gate and caption maximize/restore dispatch.
- Builder custom Close reaches the real idle/active cleanup guard, including
  repeated requests, successful/failed worker cleanup and credential disposal.
- PNG/ICO are copied through real builds and hashed in 16-entry asset manifests;
  native decoding is mocked. Cache detects and repairs ICO byte drift. Invalid
  paths, traversal, executable extensions and empty mocked-decode input fail.
- Both XAML caption structures are checked for resizing, hit testing and labels;
  work-area bounds are exercised at simulated 100/150/200% sizes. This does NOT
  prove layout, accessibility, native event routing or actual DPI handling.
- Existing model/power/hash/password/BitLocker/recovery, transaction/lock, task
  cleanup, credential exclusion and shared Medela permission regressions pass.

PASS: git diff --check; all PS1/PSD1 sources parse and all XAML is well formed.
NOT RUN: Tests/V4/Test-WindowsUI.ps1 (Windows PowerShell 5.1 / STA required).
That separate inert smoke script loads actual WPF resources, default/custom icon
and caption click routing/Closing guards. It does not stage BIOS or create tasks.
WindowsRegistryIntegration was not run. No real Dell/content-prep EXE, endpoint
firmware, BitLocker, scheduled task, restart or Intune assignment was changed.

Required Windows pilot: both flag states and old presets with your real custom
PSADT 4.1 ZIP, PNG/ICO decode/rendering, builder cleanup/console exit, mixed DPI and
small displays at 100/150/200%, keyboard/hover/focus/Narrator/high contrast, live
SYSTEM user-session prompts, real v3 accepted-source migration and task retries,
Intune runtime/detection/restart policy, NTFS parent/sibling preservation and the
existing hardware safety/recovery gates. Follow OPERATIONS.md before rollout.
The disabled incoming package waits for accepted work to resolve; it does not
rewrite an old package in place. Remove competing old Intune assignments so they
cannot offer an earlier policy. Main/v2/v3 are preserved; these changes are on v4.

Previous validation records follow.

V3.1 Schedule Install validation - 2026-09-17

PASS: 465 assertions across 11 fresh PowerShell 7.4.7 processes on Linux:
  54  Tests/Test-Deployment.ps1
  89  Tests/Test-Builder.ps1
  21  Tests/Test-BuilderClose.ps1
  14  Tests/V2/Test-Guards.ps1
  30  Tests/V2/Test-Cache.ps1
  42  Tests/V2/Test-SimpleDeployment.ps1
  13  Tests/V2/Test-Migration.ps1
  57  Tests/V2/Test-Live.ps1
  52  Tests/V2/Test-CachePermissions.ps1
  69  Tests/V2/Test-Scheduling.ps1
  24  Tests/V2/Test-ScheduledRecovery.ps1
Counts include parser/fixture assertions, not independent Windows E2E scenarios.
The schedule/recovery/flow suites were rerun after the final cleanup ordering fix.

Reproduce from Dell-BIOS-PSADT41, running each suite in its own process:
  pwsh -NoProfile -File <path above>
No Dell or Microsoft packaging EXE is executed by these portable tests.

New coverage exercises actual state, package copying, file hashes, locks,
response parsing and callbacks with inert Windows task/ACL/signature APIs:
- Schedule and reschedule retain the original deadline, one task and one private
  source copy; deferred notices retain an existing appointment.
- UTC task boundaries, missed/no-user/power holds, overdue retention, reminder
  cooldown and due appointments progressing without another consent notice.
- Only complete private custom source is published; interrupted copying and
  source/destination overlap fail without exposing/deleting unrelated files.
- Invalid UI replies, duplicate markers, malformed/past/out-of-window picker
  selections and SYSTEM-side validation refuse a schedule.
- Registration failure preserves intent; a missing/stale task is repaired from
  state; a conflicting task is not overwritten or removed.
- Staging retires the install task before the restart countdown. Existing
  asynchronous progress, power/restart/BitLocker guards continue to pass.
- Real verifier main block: passed/failed BIOS after protection recovery permit
  snapshot cleanup; same boot, recovery failure and package contention preserve
  recovery. Snapshot deletion failures keep metadata and verifier retries;
  interrupted final directory cleanup also completes on the next verifier run.
- Already-current code does not delete its own running framework. Generated
  detection remains pending until retained private-source cleanup is complete.
- Builder includes all 13 managed runtime files, preserves custom framework
  integration, and still excludes credentials/password-file hashes from records.

PASS: git diff --check. All PS1/PSD1 sources parse; XAML is well formed.
WindowsRegistryIntegration was not run. This does not validate Windows Task
Scheduler/CIM XML registration, actual native PSADT StdOut/session launch,
Windows PowerShell 5.1/.NET ACL inheritance, WPF layout/DPI/accessibility, DST
picker rendering or a real BIOS flash. Follow OPERATIONS.md for Windows pilots.
Rescheduling is available when a deployment attempt offers the install notice;
there is no persistent tray or standalone privileged scheduling entry point.
Keep Intune on No specific action and review the custom framework's restart
behavior. A task retries prerequisites but cannot run while Windows is off.

Previous validation records follow.

V3.0.1 shared Medela permission boundary validation - 2026-09-17

PASS: 170 assertions in three fresh PowerShell 7.4.7 processes on Linux:
  52  Tests/V2/Test-CachePermissions.ps1
  30  Tests/V2/Test-Cache.ps1
  88  Tests/Test-Builder.ps1

The new permission suite executes the actual parent guard, ACL-construction
logic, initializer, recursive child handling and path boundary. It uses real
inert directories with modeled Windows security objects/native ACL calls.
Covered: parent/sibling ACL and owner preservation, sibling content unchanged,
all writes confined to DellBIOS, protected directory-creation requests, separate
private/UI grants, repeat initialization, creation/write-attribute/inherit-only
grants accepted, parent-replacement rights/null DACL/untrusted owner rejected,
SID/rights diagnostics, no permission writes on rejection, direct helper path
escape rejection and no explicit ACL on a newly created shared parent.
Existing tests verify actual cache/version repair and generated package syntax,
hashes/detection, credentials, extraction and framework integration.

Reproduce from Dell-BIOS-PSADT41:
  pwsh -NoProfile -File ./Tests/V2/Test-CachePermissions.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Cache.ps1
  pwsh -NoProfile -File ./Tests/Test-Builder.ps1

No actual Windows ACL or endpoint permission was changed. Windows PowerShell
5.1/.NET Framework security-at-creation, NTFS inheritance/effective access,
SYSTEM ownership, UI user access and real shared application compatibility still
require a Windows pilot. Follow OPERATIONS.md: Shared Medela folder permissions.
Other unchanged firmware/live UI suites were not rerun for this ACL-only edit.

Previous v3.0 builder Close validation - 2026-09-17

PASS: 109 assertions in two fresh PowerShell 7.4.7 processes on Linux:
  88  Tests/Test-Builder.ps1
  21  Tests/Test-BuilderClose.ps1

The close regression runs the actual Close/Escape/Closing/timer/finally callbacks
against inert controls and real asynchronous PowerShell workers. It checks idle
exit, unrelated keys, repeated close requests, pending work left running, success
and failure cleanup before close, disposed worker credentials, recorded results,
timer shutdown and password-box clearing. No executable or firmware is run.

PASS: git diff --check. The builder XAML parses in the existing builder suite.
Reproduce from Dell-BIOS-PSADT41:
  pwsh -NoProfile -File ./Tests/Test-Builder.ps1
  pwsh -NoProfile -File ./Tests/Test-BuilderClose.ps1

Windows PowerShell 5.1/WPF launch, footer layout at different DPI, real button/
Escape/title-bar X dispatch, return to the launching terminal and actual content
prep remain manual Windows pilot checks. A queued close deliberately waits for
an active build; it does not forcibly terminate a hung external packaging tool.
V3 changes the packaging UI and builder metadata only; deployed runtime code,
file tattoos and firmware safeguards remain those of v2.3. Main/v2 are preserved.

Previous v2.3 baseline validation (not rerun in full for this builder-only edit):
V2.3 progress, guarded countdown and Medela cache validation - 2026-09-17

PASS: 279 assertions across seven fresh PowerShell 7.4.7 processes on Linux:
  49  Tests/Test-Deployment.ps1
  88  Tests/Test-Builder.ps1
  14  Tests/V2/Test-Guards.ps1
  30  Tests/V2/Test-Cache.ps1
  28  Tests/V2/Test-SimpleDeployment.ps1
  13  Tests/V2/Test-Migration.ps1
  57  Tests/V2/Test-Live.ps1
Counts include parsing and fixture assertions; these are not 279 independent
Windows end-to-end scenarios. Optional WindowsRegistryIntegration was not run.

Covered by actual code with inert files and mocked Windows boundaries:
- All PS1/PSD1 sources parse; frontend and builder XAML are well formed.
- Registry create-once state writer, damaged-state errors, secret handling,
  native argument escaping, AC and strict 50% vs 51% battery gates, optional
  runtime telemetry limits and compatibility with earlier configuration.
- Builder extraction and PSADT AST integration preserve custom module bytes,
  metadata, extensions and bootstrap without executing template code. Includes
  the Windows PowerShell 5.1 dictionary-sort compatibility regression.
- Actual asynchronous GUI worker scriptblock rejects denied engine loading with
  actionable, sanitized diagnostics. This does not reproduce Windows policy.
- New builds generate simple policy, first-line version tattoos and the approved
  runtime manifest. Source builds do not package the retired scheduler.
- Copied BIOS hashes feed generated settings/scripts. Local password round trips;
  presets, manifests, progress and logs exclude it and its file hash. No secret
  is copied into the public runtime/UI cache.
- ZIP path traversal, rooted/ADS/reserved/colliding/link entries, embedded secret,
  unsupported/ambiguous templates, duplicate functions and settings validation.
- Inert optional content-prep orchestration, tool signer rejection and cleanup.
  No Microsoft utility or Dell executable was actually run.
- Ten generated detection scenarios in isolated PowerShell runspaces: compliant
  current/newer BIOS, below-target firmware, staging, unresolved/incomplete
  transaction, suspended protection, wrong model, changed and missing runtime.
  Detection reads expected runtime hashes, requesting repair even when BIOS is
  already current; staging/enrollment alone cannot satisfy detection.
- Real cache file IO: initial copies, skip exact matches without rewriting them,
  older/unversioned/missing/drifted files, refuse newer versions, reject modified
  source manifests and path/secret injection, verify binary branding assets.
- Interrupt the second file replacement: installed manifest remains unchanged;
  the next invocation identifies and repairs the remaining file. Original state
  bytes survive refresh. Per-file replacement is atomic; the whole file set is
  not a single filesystem transaction.
- Unresolved firmware or protection ownership blocks code replacement. Unchanged
  runtime can prompt for restart of a matching already-staged transaction.
- Actual one-shot orchestration: no user/no new deadline, first prompt attempt,
  unlimited repeated deferral without extension, reminder cooldown, missing
  enrolled state failure, stage once, interrupted warning, guarded Restart Now, power
  holds, no reflash on retries, actual BIOS plus protection before success, and
  no completion while protection ownership remains unresolved.
- UI callbacks with inert controls: overdue Defer removal, normal close blocked,
  visible overdue timeout requests preparation, earlier timeout defers, SYSTEM
  overdue flag survives a backward UI clock change; unprivileged UI never
  performs a restart itself. Days remaining and Defer expiry are explicit.
- Real legacy state/locks with task/process mocks: reject active/staged firmware,
  unsafe phases, held old firmware lock and unexpected task actions; import the
  exact expired deadline/window; retire only the two known tasks and exact UI
  process; remove old Program Files UI, retain protected diagnostic data; hold
  locks through the transition, refuse reappearing tasks, preserve retry state.
- Actual restart guards with BitLocker mocks: wrong boot/hash/status, missing
  transaction, unowned suspension, locked drive and unexpected encryption fail.

- Actual PSADT asynchronous-result adapter: incomplete LaunchInfo.Task, completed
  results, fast ProcessResult, unsupported handles, unknown exit codes and
  faulted tasks. Worker-result failure still cleans temporary UI. Official
  PSADT 4.1.0 Start-ADTProcess/Start-ADTProcessAsUser source was inspected; custom
  framework execution remains a Windows pilot boundary.
- Actual progress loop with real TaskCompletionSource and status-file writes:
  animated Preparing status, UI failure before launch blocks staging, UI loss
  during staging never kills the worker, real retry/result code retained.
- Actual SYSTEM countdown and restart function with the native shutdown command
  mocked: one-hour expiry, 15/30/45-minute reminder sequence, early Restart Now,
  firmware lock and final guards, 51% accepted, 50% and AC loss cancelled.
- Sleep bias, monitoring gap, forward/backward clock changes, session change,
  unexpected UI exit and Windows restart refusal cancel the timer; temporary
  UI data is removed while original deadlines and recovery files survive.
- An abandoned persisted restart deadline never causes an immediate catch-up
  restart; a new attempt gives a fresh full warning. Crash leftovers are removed
  at the next package invocation. Actual verifier cleanup removes only UI data
  from its transaction's previous boot, preserving current-boot UI and state.
- Actual UI callbacks: preparation activity, countdown text, Minimize and X keep
  the countdown alive, one reminder per interval, no restart action while
  preparing, Restart Now returns only a request code, stale heartbeat closes UI.
  Preview counts down/reminds and closes without requesting any actual restart.

PASS: git diff --check.

Reproduce from Dell-BIOS-PSADT41, in separate processes:
  pwsh -NoProfile -File ./Tests/Test-Deployment.ps1
  pwsh -NoProfile -File ./Tests/Test-Builder.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Guards.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Cache.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-SimpleDeployment.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Migration.ps1
  pwsh -NoProfile -File ./Tests/V2/Test-Live.ps1

On Windows, additionally run all suites using powershell.exe, then:
  powershell.exe -NoProfile -File .\Tests\Test-Deployment.ps1 -WindowsRegistryIntegration
  powershell.exe -NoProfile -STA -File .\Files\UI\Show-BiosUI.ps1 -Demo
  powershell.exe -NoProfile -STA -File .\Files\UI\Show-BiosUI.ps1 -Demo -Mode Progress
  powershell.exe -NoProfile -STA -File .\Files\UI\Show-BiosUI.ps1 -Demo -Mode Restart
  .\Builder\Start-PackageBuilder.cmd

NOT TESTED / WINDOWS PILOT REQUIRED:
- Windows PowerShell 5.1 execution, WPF rendering/scaling/accessibility, GUI
  browse/password/preset/async behavior, standard-user session launch via the
  chosen PSADT 4.1.x framework, sign-out/lock/timeout behavior and real UI exits.
- Windows ownership/ACL inheritance and reparse rejection, particularly a shared
  pre-existing Medela parent. Security boundaries were mocked, not certified.
- Real legacy scheduled-task/process retirement, Windows file replacement/locking,
  recovery-task registration and boot-time execution, SYSTEM privileges.
- Native QueryUnbiasedInterruptTime/GetTickCount64 behavior on sleep, hibernate
  and Modern Standby; actual WPF sound, restore/recenter/activation, multi-monitor
  DPI and session lock/logoff; Windows delete-sharing and atomic file replacement.
- Real Authenticode chains, custom framework behavior, signed final scripts and
  content-prep execution; no deployable firmware or .intunewin was produced here.
- Intune detection/retry/timeout, actual file repair on a current-BIOS device,
  assignment migration, competing tenant restart policies and offline/sleep cases.
  Microsoft documents interactive Intune installations and user-session UI
  workarounds as unsupported. A successful PSADT pilot does not remove that
  platform support limitation. See:
  https://learn.microsoft.com/en-us/intune/app-management/deployment/add-win32#step-2-program
- Real Dell EXE/model/password, staging/boot/actual firmware verification, physical
  battery/AC/runtime telemetry, escrow/retrieval and BitLocker suspension/resume.
  Never interrupt a live BIOS updater to test failure paths.

Behavioral limits intentionally documented in README.md and OPERATIONS.md:
- No resident controller or exact-date scheduling. Intune supplies the next
  invocation; reminder hours are a cooldown, not a guaranteed notification time.
- Deadline starts at the first active-user prompt launch ATTEMPT, not proof of
  visible display or reading; launch failure/retry cannot reset it.
- One live SYSTEM restart countdown (60 minutes by default) after staging; no
  restart task or future OS shutdown timer. Expiry or Restart Now requests a
  guarded /r /t 0 without /f, so applications may block it. Safety interruption
  cancels the countdown; never delete the staged transaction or verifier then.
  Power-off cleanup waits for the post-boot verifier or a later package attempt.
  Monitor staged updates and outstanding owned suspension.
- File tattoo/hash identify version and integrity, not publisher authenticity.
  The reviewed package and enterprise signing policy establish runtime trust.
  After signing runtime scripts, regenerate both manifest and Intune detection.
- New files use ProgramData\Medela\DellBIOS. Old protected ProgramData diagnostic
  records and firmware transaction identity remain for recovery continuity.

MANUAL EXAMPLE CONFIGURATION BLOCKERS (unchanged; do not deploy as-is):
- Example SHA256 is 65 characters: recalculate from the approved EXE; do not trim.
- PB14250 model vs PA13250-named executable compatibility is unverified.
- PackageReviewed=false; real BIOS EXE/password/PSADT distribution are absent.
The builder replaces these sample values from selected reviewed inputs and copied
firmware. Test fixtures are inert and must never be deployed.

No production firmware, BitLocker state, task, restart or Intune assignment was
changed by these checks. Main remains v1; changes belong to v2.
