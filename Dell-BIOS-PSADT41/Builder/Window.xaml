<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dell BIOS Package Builder v4" Width="920" Height="740" MinWidth="560" MinHeight="460" WindowStyle="None" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}"
        FontFamily="Segoe UI" FontSize="14" UseLayoutRounding="True">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
  </shell:WindowChrome.WindowChrome>

  <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"><Grid>
    <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
      <Grid Height="48"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Grid Margin="14,0,8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="24"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
          <Image Width="24" Height="24" Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=Window}}" AutomationProperties.Name="Application icon"/>
          <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"/>
        </Grid>
        <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
          <Button x:Name="CaptionMinimize" shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CaptionButton}" Content="&#x2014;" ToolTip="Minimize window" AutomationProperties.Name="Minimize window"/>
          <Button x:Name="CaptionMaximize" shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CaptionButton}" Content="&#x25A1;" ToolTip="Maximize window" AutomationProperties.Name="Maximize window"/>
          <Button x:Name="CaptionClose" shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CaptionButton}" Content="&#x00D7;" ToolTip="Close window" AutomationProperties.Name="Close window"/>
        </StackPanel>
      </Grid>
    </Border>

    <Border Grid.Row="1" Background="{DynamicResource AccentBrush}" Padding="24,15">
      <StackPanel>
        <TextBlock Text="DEVICE CARE / PACKAGING" Foreground="{DynamicResource AccentTextBrush}" FontWeight="SemiBold" FontSize="12"/>
        <TextBlock Text="Build your Dell BIOS deployment" Foreground="{DynamicResource AccentTextBrush}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,5"/>
        <TextBlock Text="Your approved BIOS. Your PSADT 4.1 framework. One repeatable package." Foreground="{DynamicResource AccentTextBrush}"/>
      </StackPanel>
    </Border>
    <TabControl x:Name="Tabs" Grid.Row="2" Margin="16,12,16,10" Background="{DynamicResource SurfaceBrush}">
      <TabItem Header="1  Files"><ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel x:Name="FilesPanel" Margin="22"/></ScrollViewer></TabItem>
      <TabItem Header="2  Deployment"><ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel x:Name="DeploymentPanel" Margin="22"/></ScrollViewer></TabItem>
      <TabItem Header="3  Experience"><ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel x:Name="ExperiencePanel" Margin="22"/></ScrollViewer></TabItem>
      <TabItem Header="4  Build"><ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel Margin="22">
        <TextBlock Text="Review and build" FontSize="23" FontWeight="SemiBold"/>
        <TextBlock Text="The builder does not run the BIOS updater. Test each model, firmware version and customized framework on Windows before deployment." Margin="0,10,0,14"/>
        <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" Padding="18"><TextBlock x:Name="ReviewText" FontFamily="Consolas" FontSize="13"/></Border>
        <CheckBox x:Name="Reviewed" AutomationProperties.Name="Confirm package review">
          <TextBlock Text="I reviewed Dell compatibility, prerequisite versions and the trusted PSADT template. These settings are approved for a pilot."/>
        </CheckBox>
        <TextBlock Text="The package contains the shared BIOS password in a local data file. Output is restricted to your Windows account, SYSTEM and Administrators. Protect both Source and .intunewin; do not upload them to Git." FontSize="13" Foreground="{DynamicResource MutedBrush}" Margin="0,8,0,12"/>
        <WrapPanel><Button x:Name="BuildButton" Content="_Build package" Style="{DynamicResource PrimaryAction}"/><Button x:Name="OpenButton" Content="Open _output" IsEnabled="False"/></WrapPanel>
        <ProgressBar x:Name="Progress" Height="5" Margin="0,16,0,10" Visibility="Collapsed"/>
        <TextBox x:Name="BuildLog" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="150" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Build status" AutomationProperties.LiveSetting="Polite"/>
      </StackPanel></ScrollViewer></TabItem>
    </TabControl>
    <Border Grid.Row="3" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,9" Background="{DynamicResource SurfaceBrush}">
      <StackPanel>
        <TextBlock Text="Presets save settings only. Enter the password again for each build." Foreground="{DynamicResource MutedBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <WrapPanel HorizontalAlignment="Right"><Button x:Name="LoadButton" Content="_Load preset"/><Button x:Name="SaveButton" Content="_Save preset"/><Button x:Name="CloseButton" Content="_Close" AutomationProperties.Name="Close package builder" ToolTip="Close the builder. A running build finishes and cleans up before closing."/></WrapPanel>
      </StackPanel>
    </Border>
  </Grid></Border>
</Window>
