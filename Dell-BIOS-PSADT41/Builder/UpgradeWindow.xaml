<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
 Title="Replace install file" Width="920" Height="760" MinWidth="560" MinHeight="440" WindowStyle="None" ResizeMode="CanResize" WindowStartupLocation="CenterOwner" FontFamily="Segoe UI" FontSize="14" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}" UseLayoutRounding="True">
 <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" GlassFrameThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
 <Grid><Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Border Background="{DynamicResource SurfaceBrush}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <StackPanel Orientation="Horizontal" Margin="14,0"><Image Width="24" Height="24" Source="{Binding Icon,RelativeSource={RelativeSource AncestorType=Window}}"/><TextBlock Text="Replace install file" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/></StackPanel>
   <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
    <Button x:Name="CaptionMinimize" Style="{DynamicResource CaptionButton}" Content="&#x2014;" AutomationProperties.Name="Minimize"/>
    <Button x:Name="CaptionMaximize" Style="{DynamicResource CaptionButton}" Content="&#x25A1;" AutomationProperties.Name="Maximize"/>
    <Button x:Name="CaptionClose" Style="{DynamicResource CaptionButton}" Content="&#x00D7;" AutomationProperties.Name="Close upgrade assistant"/>
   </StackPanel>
  </Grid></Border>
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"><StackPanel Margin="20">
   <TextBlock Text="Upgrade a working copy" FontSize="24" FontWeight="SemiBold"/>
   <TextBlock Text="Choose the old installer and its replacement. Review proposed edits, then create a new complete package ZIP. Your original files remain intact." TextWrapping="Wrap" Margin="0,8,0,12"/>
   <StackPanel x:Name="Inputs">
    <Label Content="Existing _installer" Target="{Binding ElementName=OldInstaller}"/><ComboBox x:Name="OldInstaller" AutomationProperties.Name="Existing installer"/>
    <Label Content="_Replacement MSI or EXE" Target="{Binding ElementName=NewInstaller}"/>
    <DockPanel><Button x:Name="BrowseInstaller" DockPanel.Dock="Right" Content="Browse..."/><TextBox x:Name="NewInstaller" AutomationProperties.Name="Replacement installer"/></DockPanel>
    <CheckBox x:Name="KeepName" Content="Keep existing installer filename (useful for calculated references or zero-configuration MSI apps)"/>
    <Label Content="Existing _MST (optional)" Target="{Binding ElementName=OldTransform}"/><ComboBox x:Name="OldTransform" AutomationProperties.Name="Existing transform"/>
    <Label Content="Replacement MST (optional; leave blank to migrate supported Property settings)" Target="{Binding ElementName=NewTransform}"/>
    <DockPanel><Button x:Name="BrowseTransform" DockPanel.Dock="Right" Content="Browse MST..."/><TextBox x:Name="NewTransform" AutomationProperties.Name="Replacement transform"/></DockPanel>
    <Button x:Name="Analyze" Content="_Analyze replacement" HorizontalAlignment="Left" Style="{DynamicResource PrimaryAction}"/>
   </StackPanel>
   <TextBlock x:Name="Identity" TextWrapping="Wrap" Margin="0,12,0,8"/>
   <DataGrid x:Name="Edits" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" Height="190" SelectionMode="Single" AutomationProperties.Name="Proposed script changes" EnableRowVirtualization="True">
    <DataGrid.Columns>
     <DataGridCheckBoxColumn Header="Apply" Binding="{Binding Selected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="55"/>
     <DataGridTextColumn Header="Change" Binding="{Binding Kind}" IsReadOnly="True" Width="100"/>
     <DataGridTextColumn Header="Section" Binding="{Binding Phase}" IsReadOnly="True" Width="100"/>
     <DataGridTextColumn Header="Line" Binding="{Binding Line}" IsReadOnly="True" Width="45"/>
     <DataGridTextColumn Header="Before" Binding="{Binding Before}" IsReadOnly="True" Width="*"/>
     <DataGridTextColumn Header="After" Binding="{Binding After}" IsReadOnly="True" Width="*"/>
    </DataGrid.Columns>
   </DataGrid>
   <TextBlock Text="Pre-install product codes may remove the old version and are not selected automatically. Review calculated references, other MSTs, detection scripts and vendor upgrade behavior. Property migration does not prove the application will install correctly." TextWrapping="Wrap" FontSize="12" Margin="0,10,0,6"/>
   <CheckBox x:Name="Reviewed"><TextBlock Text="I reviewed the selected changes, metadata, transform and detection requirements." TextWrapping="Wrap"/></CheckBox>
   <TextBlock x:Name="Status" TextWrapping="Wrap" AutomationProperties.LiveSetting="Polite"/>
  </StackPanel></ScrollViewer>
  <StackPanel Grid.Row="2" Margin="20,8"><ProgressBar x:Name="Progress" Height="4" Visibility="Collapsed"/><WrapPanel HorizontalAlignment="Right"><Button x:Name="Apply" Content="_Create upgraded ZIP" Style="{DynamicResource PrimaryAction}" IsEnabled="False"/><Button x:Name="Close" Content="_Close"/></WrapPanel></StackPanel>
 </Grid>
</Window>
